assistant:
  name: "Sweetie"
  user_name: "Aadish"
  log_level: "INFO"

wake_word:
  # Using openWakeWord's built-in "hey_jarvis" as a placeholder for Phase 1.
  phrase: "hey_jarvis"
  threshold: 0.5

tts:
  model_path: "assets/en_US-lessac-low.onnx"
  config_path: "assets/en_US-lessac-low.onnx.json"
  speed: 1.0
